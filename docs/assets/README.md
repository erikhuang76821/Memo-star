# README assets

Drop the two marketing images here with these exact filenames — the project
README references them:

| filename | where it appears | which image |
|---|---|---|
| `banner.png` | top banner of `README.md` | the wide hero ("Code Recall — Decision Persistence Layer for AI Coding", memory-palace + lighthouse) |
| `overview.png` | mid-article, above the "Market scarcity" comparison | the dark feature overview (decision cards D-027/D-021/D-013 + session digest + the 5 feature tiles) |

Recommended: PNG, ~1600px wide, < ~500 KB each (optimize so the repo stays light).
Once added, `git add docs/assets/*.png` and commit — GitHub will render them in the README.
